import { FiltroHotelesPipe } from './filtro-hoteles.pipe';

describe('FiltroHotelesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroHotelesPipe();
    expect(pipe).toBeTruthy();
  });
});
